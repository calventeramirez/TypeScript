const name: string = "Pablo";

let hpPoints: number | "FULL" = 95; //Tambien se puede cambiar el full por string y cogeria todos los string

const isAlive: boolean = true;

hpPoints = "FULL";

console.log(name, hpPoints, isAlive);

export{};